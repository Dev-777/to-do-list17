const initialState: any = {
    name:'',
    surname:'',
    email:'',
    password:'',
}

export const signUpReducer: any = (state=initialState, actions: any)=>{

    switch(actions.type){

        default:
            return state;
    }
}

